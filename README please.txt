Please run the App_mod.py for running the news caster. for audio generated text you will be prompt accordingly.

You'll need to install these Python libraries using pip. Here are the commands to install each required package:

pip install duckduckgo_search
pip install gTTS

or 

To install all of them in one command:

pip install duckduckgo_search gTTS




